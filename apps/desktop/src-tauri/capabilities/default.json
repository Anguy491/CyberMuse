{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "M1 desktop shell with no privileged product commands",
  "windows": ["main"],
  "permissions": ["core:default"]
}
